apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: resnet-models
  namespace: mlops
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: manual
  resources:
    requests: { storage: 2Gi }

